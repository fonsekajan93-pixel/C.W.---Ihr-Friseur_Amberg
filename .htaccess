DirectoryIndex index.html
Options -MultiViews

RewriteEngine On

# Allow clean URLs such as /impressum and /datenschutz while serving
# the existing static HTML files behind the scenes.
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+?)/?$ $1.html [L]
